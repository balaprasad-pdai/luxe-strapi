apiVersion: apps/v1
kind: Deployment
metadata:
  name: strapi-app
  namespace: strapi-app
  labels:
    app: strapi-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: strapi-app
  template:
    metadata:
      labels:
        app: strapi-app
    spec:
      containers:
      - name: strapi-app
        image: populardigitalai/strapi-app:latest
        imagePullPolicy: Always
        # Environment comes from ConfigMap and Secrets (do not bake into the image)
        ports:
        - containerPort: 1337
          protocol: TCP
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 1337
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 1337
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        envFrom:
          - configMapRef:
              name: strapi-app-config